import { PropsWithChildren } from 'react';
interface AccordionProps {
    label: string;
}
declare const Accordion: ({ label, children }: PropsWithChildren<AccordionProps>) => import("react/jsx-runtime").JSX.Element;
export default Accordion;
